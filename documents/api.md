# Developer API Reference

The public API base URL is https://api.example.com.

## Authentication

Requests require a bearer token in the Authorization header. Tokens are issued by the platform team.

## Endpoints

- GET /v1/health
- POST /v1/invoices
- GET /v1/reports
